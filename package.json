{
  "name": "ppa-expliquee",
  "version": "0.1.0",
  "description": "A basic tool to explain PPA computations",
  "scripts": {
    "build": "NODE_ENV=production next build && NODE_ENV=production next export",
    "deploy": "touch out/.nojekyll && gh-pages -d out --dotfiles",
    "dev": "next",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/guillett/ppa_expliquee.git"
  },
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/guillett/ppa_expliquee"
  },
  "homepage": "https://github.com/guillett/ppa_expliquee#readme",
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "isomorphic-unfetch": "^2.0.0",
    "next": "^8.1.0",
    "next-images": "^0.10.6",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-json-view": "^1.19.1",
    "react-modal": "^3.5.1",
    "react-treebeard": "^2.1.0"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
